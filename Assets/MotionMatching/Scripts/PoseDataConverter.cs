using UnityEngine;

namespace MMSystem
{
    public class PoseDataConverter
    {
        public bool ConvertData(AnimationClip clip, Animator anim, SearchAndSaveType type) 
        {
            Debug.Log(type);
            return true;
        }
    }
}